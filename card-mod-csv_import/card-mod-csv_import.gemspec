# -*- encoding : utf-8 -*-

CardModGem.mod "csv_import" do |s, _d|
  s.version = "0.9"
  s.summary = "tool to import csv files "
  s.description = "a card mod for importing data from csv files as cards into a deck"
end
